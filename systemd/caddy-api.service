# caddy-api.service
#
# For using Caddy with the API.
#
# This unit does NOT explicitly load a Caddyfile.
# 
# See https://caddyserver.com/docs/install for instructions.

[Unit]
Description=Caddy (API mode)
Documentation=https://caddyserver.com/docs/
After=network.target network-online.target
Requires=network-online.target

[Service]
Type=notify
User=caddy
Group=caddy
# Same as caddy.service. systemd creates these directories on each start and
# gives them to the caddy user. The ownership does not depend on postinstall.sh.
LogsDirectory=caddy
LogsDirectoryMode=0755
StateDirectory=caddy
StateDirectoryMode=0755
ExecStart=/usr/bin/caddy run --environ --resume
TimeoutStopSec=5s
LimitNOFILE=1048576
LimitNPROC=512
PrivateTmp=true
ProtectSystem=full
AmbientCapabilities=CAP_NET_BIND_SERVICE

[Install]
WantedBy=multi-user.target
